package main
import "fmt"
func main() {
    x := 11 //переменная x. оператор := автоматически определяет тип переменной 
    y := 66 //переменная y
    z := "go language" //переменной z. тип переменной string, т.к присваивается строковое значение 
    fmt.Println("краткая форма объявления переменных:")
    fmt.Println("x =", x, ", y =", y, ", z =", z) //1. добавляет пробелы. 2. Вывод переменных в одной строке
}

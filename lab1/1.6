package main
import "fmt"
func main() {
	a, b, c := 10.0, 20.0, 30.0 //переменные имеют тип float64, т.к. записаны дробными числами
	avg := (a + b + c) / 3 // Вычисление среднего арифметического. 3 кол-во чисел
	fmt.Println("Среднее значение:", avg) // Вывод результата 
}
